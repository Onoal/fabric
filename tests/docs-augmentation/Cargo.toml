[package]
name = "fabric-test-docs-augmentation"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
fabric.workspace = true
fabric-test-adapter-clock-memory = { path = "../extensions/adapter-clock-memory" }
fabric-test-resource-clock = { path = "../extensions/resource-clock" }
fabric-test-system-operations = { path = "../extensions/system-operations" }
fabric-test-component-greeter = { path = "../extensions/component-greeter" }
